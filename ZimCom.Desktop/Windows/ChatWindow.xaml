<Window x:Class="ZimCom.Desktop.Windows.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:core="clr-namespace:ZimCom.Core.Models;assembly=ZimComCore"
        xmlns:local="clr-namespace:ZimCom.Desktop.Windows"
        xmlns:viewmodels="clr-namespace:ZimCom.Desktop.ViewModels"
        xmlns:globalization="clr-namespace:System.Globalization;assembly=System.Runtime"
        d:DataContext="{d:DesignInstance Type=viewmodels:ChatWindowViewModel}"
        mc:Ignorable="d"
        Title="Chat and Messages" Height="450" Width="800" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <CompositeCollection x:Key="EmptyCollection" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ListView Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch" Margin="4" ItemsSource="{Binding Messages,
                   FallbackValue={StaticResource EmptyCollection}}">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type core:ChatMessage}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Text="{Binding DateTime, StringFormat={}{0:G},
                                        ConverterCulture={x:Static globalization:CultureInfo.CurrentCulture}}" />
                        <TextBlock Text=" " />
                        <TextBlock Text="{Binding User.Label}" />
                        <TextBlock Text=" " />
                        <TextBlock Text=":" />
                        <TextBlock Text=" " />
                        <TextBlock Text="{Binding Message}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBox Grid.Row="1" Grid.Column="0" HorizontalAlignment="Stretch" Margin="4" TextWrapping="Wrap"
                 Text="{Binding CurrentMessage}"
                 IsEnabled="{Binding ChatEnabled}"/>
        <Button Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" Margin="4"
                FontFamily="{DynamicResource SymbolThemeFontFamily}" Content="&#xE8BD;"
                Command="{Binding SendMessageCommand}"
                IsEnabled="{Binding ChatEnabled}"
                ToolTip="Send a message to channel" IsDefault="True"/>
    </Grid>
</Window>
